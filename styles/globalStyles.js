import { StyleSheet } from 'react-native';

export const globalStyles = StyleSheet.create({
    container: {
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center',
        width: '100%'
    },
    button: {
        margin: 8
    },
    textTitle: {
        fontSize: 18,
        fontWeight: 'bold',
        letterSpacing: 1
    },
    textContent: {
        fontSize: 14
    }
});